# Compose interpolation only. Generate a hexadecimal password; never commit .env.
POSTGRES_PASSWORD=replace-with-a-random-hexadecimal-password
LOCKGATE_ORIGIN=http://localhost:8080
LOCKGATE_INSECURE_DEV_COOKIES=true
